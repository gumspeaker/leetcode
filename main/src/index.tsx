import React from "react";
import ReactDOM from "react-dom/client";
import {} from "leet-code-answer";
ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <div>ws</div>
  </React.StrictMode>
);
